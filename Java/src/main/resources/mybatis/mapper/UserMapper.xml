<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vitaheng.basic.mybatis.mapper.UserMapper">

    <select id="queryUserById" parameterType="Long" resultType="User">
      select * from tb_user where id=#{id}
    </select>
    <select id="queryUserList" resultType="User">
        select * from tb_user
    </select>
    <insert id="insertUser" parameterType="User">
        insert into tb_user values(null,#{username},#{password},#{name},#{age},#{sex},#{birthday},now(),now())
    </insert>
    <update id="updateUser" parameterType="User">
        update tb_user set username=#{username},password=#{password},name=#{name},age=#{age},sex=#{sex},birthday=#{birthday},updated=now() where id=#{id}
    </update>
    <delete id="deleteById" parameterType="Long">
        delete from tb_user where id=#{id}
    </delete>
</mapper>